training:
  seed: 42
  batch_size: 8
  learning_rate: 0.0003
  max_steps: 1000
  eval_every: 50
  save_every: 100
  grad_clip: 1.0
  mixed_precision: true
  resume: false
  checkpoint_dir: outputs/checkpoints
  output_dir: outputs
